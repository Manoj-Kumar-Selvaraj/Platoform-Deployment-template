apiVersion: v2
name: jenkins-platform
description: Jenkins deployment wrapper for Platform MVP
type: application
version: 0.1.0
appVersion: "lts"

dependencies:
  - name: jenkins
    version: "5.*"
    repository: "https://charts.jenkins.io"
